{{- with .Values.app.components.crawler }}
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ $.Release.Namespace | quote }}
  name: {{ include "app.components.crawler.fullname" $ }}
  labels:
    {{- include "app.components.crawler.labels" $ | nindent 4 }}

  {{- with $.Values.common.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .replicas }}
  selector:
    matchLabels:
      {{- include "app.components.crawler.selectorLabels" $ | nindent 6 }}
  template:
    metadata:
      {{- with .annotations }}
      annotations:
        {{ toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "app.components.crawler.labels" $ | nindent 8 }}
    spec:
      serviceAccountName: {{ include "common.serviceAccount.name" $ }}
      automountServiceAccountToken: {{ default false .automountServiceAccountToken }}
      {{- with (default $.Values.global.imagePullSecrets $.Values.imagePullSecrets) }}
      imagePullSecrets:
        {{- range . }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      {{- with .securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: crawler
          image: {{ include "app.image" $ }}
          imagePullPolicy: {{ $.Values.app.image.pullPolicy }}
          {{- with .container.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "common.fullname" $ }}
          ports:
            - name: grpc
              protocol: TCP
              containerPort: 50051
          livenessProbe:
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
            tcpSocket:
              port: grpc
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            exec:
              command: ["/bin/sh", "-c", "grpc_health_probe -addr localhost:50051"]
          {{- with .container.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
